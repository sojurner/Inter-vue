### Write a function that, given a sentence, finds the corresponding closing parenthesis.

###### Example:

Input: (`"Sometimes (when I nest them (my parentheticals) too much (like this (and this))) they get confusing."`, 10)
Output: 79
